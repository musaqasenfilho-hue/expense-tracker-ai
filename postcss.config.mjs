/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // Enables Tailwind utility generation from scanned source files.
    tailwindcss: {},
  },
};

export default config;
