/** @type {import('next').NextConfig} */
// Central place for Next.js runtime/build flags.
// Keep minimal until a concrete need appears (images, headers, rewrites, etc.).
const nextConfig = {};

export default nextConfig;
